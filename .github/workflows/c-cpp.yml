name: C/C++ CI

on:
  push:
    branches: [ "branchHTTPsever" ]
  pull_request:
    branches: [ "branchHTTPsever" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4   
    - name: configure
    - run: ./configure
    
    - name: make
      run: make
   
