name: C/C++ CI

on:
  push:
    branches: [ "branchHTTPsever" ]
  pull_request:
    branches: [ "branchHTTPsever" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Configure
      run: ./configure  

    - name: Build
      run: make  

    - name: Run tests
      run: |
        ./my_program  
